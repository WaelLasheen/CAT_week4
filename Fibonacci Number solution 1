class Solution {
public:
    int fib(int n) {
        return Fibonacci(n);
    }
     int Fibonacci(int x){
        if(x==0)
        return 0;
        else if(x==1)
        return 1;
        else
        return Fibonacci(x-1)+Fibonacci(x-2);
     }
};
